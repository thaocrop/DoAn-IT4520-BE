export enum ContactSortFields {
  FULL_NAME = 'fullName',
  TEL = 'phoneNumber',
  POINTS = 'user.points',
  SALES = 'user.totalSales',
  TIER = 'user.memberTier',
  CREATED_AT = 'createdAt',
}

export enum ContactSortValues {
  DESC = 'DESC',
  ASC = 'ASC',
}
