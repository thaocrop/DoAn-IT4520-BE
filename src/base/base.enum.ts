export enum Status {
  ACTIVE = 'ACTIVE',
  INACTIVE = 'INACTIVE',
  DELETED = 'DELETED',
}

export enum StatusEnum {
  Active = 'ACTIVE',
  InActive = 'INACTIVE',
}

export enum RequestHeadersEnum {
  Authorization = 'authorization',
}
